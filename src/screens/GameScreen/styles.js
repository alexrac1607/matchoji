import { StyleSheet } from 'react-native'
import config from '../../styles/config'

const styles = StyleSheet.create({
  background: {
    backgroundColor: config.colororange,
    height: '100%',
    background: '',
    width: '100%',
  },
  backgroundImg: {
    height: '100%',
    opacity: 0.5,
  },
})
export default styles
